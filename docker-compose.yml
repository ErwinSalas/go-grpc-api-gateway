version: '3'
services:
  auth-service:
    container_name: api-gateway
    build: .
    ports: 
      - 3000:3000
    restart: on-failure
    volumes:
      - ./:/usr/src/app-auth/
    networks:
      - fullstack
networks:
  fullstack:
    external: true

volumes:
  api:
  database_mysql:

